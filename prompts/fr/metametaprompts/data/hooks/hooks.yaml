# Hooks Source of Truth
# ============================================================================
# Source de vérité pour tous les hooks du projet Meta-prompt-LLM.
#
# FLUX DE DONNÉES:
#   1. Ce fichier (data/hooks/hooks.yaml) = Source de vérité
#   2. data-sync copie vers .ai/hooks/hooks.yaml
#   3. generate.sh génère les configs pour TOUTES les plateformes:
#      - .claude/settings.json (Claude Code)
#      - .cursor/hooks.json (Cursor)
#      - .opencode/hooks/*.sh (OpenCode)
#      - codex.toml (Codex CLI)
#      - .aider.conf.yml (Aider - limité)
#      - .continuerc.json (Continue.dev - limité)
#
# LIFECYCLE EVENTS (ordre d'exécution):
#   1. SessionStart     - Avant toute interaction utilisateur·ice
#   2. UserPromptSubmit - Quand l'utilisateur·ice envoie un message
#   3. PreToolUse       - Avant l'exécution d'un outil (peut bloquer)
#   4. PostToolUse      - Après l'exécution d'un outil
#   5. Stop             - Quand Claude finit de répondre
#   6. SessionEnd       - Quand la session se termine
#
# HOOK TYPES:
#   - command: Exécute une commande shell (exit 0 = succès)
#   - prompt:  Question oui/non à l'utilisateur·ice
#   - agent:   Sous-agent avec accès aux outils (Read, Grep, Glob)
#
# VISUAL FEEDBACK CONVENTION:
#   Les hooks utilisent des préfixes emoji pour indiquer l'agent actif:
#   🔧 [data-sync] - Agent de synchronisation
#   🧠 [memory-keeper] - Agent mémoire
#   🔄 [self-improver] - Agent d'amélioration
#   ✅ [consistency-check] - Vérification de consistance
#   📊 [workflow-orchestrator] - Orchestrateur
#
# PLATFORM SUPPORT MATRIX:
#   ┌─────────────┬──────────────┬─────────────────────────────────────────┐
#   │ Platform    │ Hooks Level  │ Limitations                             │
#   ├─────────────┼──────────────┼─────────────────────────────────────────┤
#   │ Claude Code │ ★★★★★ Full   │ None - all 6 events + agent hooks       │
#   │ Cursor      │ ★★★★☆ Good   │ No SessionStart, no agent hooks         │
#   │ OpenCode    │ ★★★☆☆ Medium │ Requires oh-my-opencode plugin          │
#   │ Codex CLI   │ ★★☆☆☆ Basic  │ Only notify on agent-turn-complete      │
#   │ Aider       │ ★☆☆☆☆ Minimal│ No hooks, only auto_lint/test_cmd       │
#   │ Continue    │ ★☆☆☆☆ Minimal│ Data events only, no command hooks      │
#   └─────────────┴──────────────┴─────────────────────────────────────────┘
#
# ============================================================================

version: "2.1.0"
updated: "2026-02-02"

# ============================================================================
# SESSION START - Initialisation AVANT toute interaction
# ============================================================================
# Runs when a session begins. source = "startup" | "resume" | "clear" | "compact"
# Output goes to additionalContext for Claude to see.

SessionStart:
  # ----------------------------
  # 0. Platform detection and limitations display
  # ----------------------------
  - name: "init-platform-detect"
    description: "Detect LLM platform and show capabilities/limitations"
    enabled: true
    hooks:
      - type: "command"
        timeout: 5
        command: |
          echo ""
          echo "╔═══════════════════════════════════════════════════════════════╗"
          echo "║              META-PROMPT-LLM - Platform Detection             ║"
          echo "╚═══════════════════════════════════════════════════════════════╝"
          echo ""

          # Detect platform based on environment variables and config files
          PLATFORM="unknown"
          PLATFORM_LEVEL="?"
          LIMITATIONS=""

          # Claude Code detection
          if [ -n "$CLAUDE_CODE_VERSION" ] || [ -f ".claude/settings.json" ]; then
            PLATFORM="Claude Code"
            PLATFORM_LEVEL="★★★★★ Full"
            LIMITATIONS="Aucune - support complet (6 events + agent hooks)"
          # Cursor detection
          elif [ -n "$CURSOR_VERSION" ] || [ -f ".cursor/hooks.json" ]; then
            PLATFORM="Cursor"
            PLATFORM_LEVEL="★★★★☆ Good"
            LIMITATIONS="Pas de SessionStart, pas de hooks agent"
          # OpenCode detection
          elif [ -n "$OPENCODE_VERSION" ] || [ -d ".opencode" ]; then
            PLATFORM="OpenCode"
            PLATFORM_LEVEL="★★★☆☆ Medium"
            LIMITATIONS="Nécessite plugin oh-my-opencode pour hooks complets"
          # Codex detection
          elif [ -n "$CODEX_VERSION" ] || [ -f "codex.toml" ]; then
            PLATFORM="Codex CLI"
            PLATFORM_LEVEL="★★☆☆☆ Basic"
            LIMITATIONS="Seul event: agent-turn-complete (notify)"
          # Aider detection
          elif [ -n "$AIDER_VERSION" ] || [ -f ".aider.conf.yml" ]; then
            PLATFORM="Aider"
            PLATFORM_LEVEL="★☆☆☆☆ Minimal"
            LIMITATIONS="Pas de hooks, seulement auto_lint et test_cmd"
          # Continue detection
          elif [ -n "$CONTINUE_VERSION" ] || [ -f ".continuerc.json" ]; then
            PLATFORM="Continue.dev"
            PLATFORM_LEVEL="★☆☆☆☆ Minimal"
            LIMITATIONS="Data events HTTP uniquement, pas de hooks commande"
          # Fallback - try to detect from running process
          elif [ -n "$TERM_PROGRAM" ]; then
            case "$TERM_PROGRAM" in
              *cursor*|*Cursor*)
                PLATFORM="Cursor"
                PLATFORM_LEVEL="★★★★☆ Good"
                LIMITATIONS="Pas de SessionStart, pas de hooks agent"
                ;;
              *)
                PLATFORM="Terminal ($TERM_PROGRAM)"
                PLATFORM_LEVEL="? Unknown"
                LIMITATIONS="Plateforme non reconnue - fonctionnalités limitées possibles"
                ;;
            esac
          fi

          echo "🖥️  Plateforme détectée: $PLATFORM"
          echo "📊 Niveau de support:   $PLATFORM_LEVEL"
          echo ""

          if [ "$PLATFORM" != "Claude Code" ]; then
            echo "⚠️  LIMITATIONS:"
            echo "   $LIMITATIONS"
            echo ""
            echo "💡 Pour un support complet des hooks, utilisez Claude Code."
            echo "   https://claude.ai/code"
          else
            echo "✅ Support complet des hooks activé"
          fi

          echo ""
          echo "───────────────────────────────────────────────────────────────────"
          echo ""

  # ----------------------------
  # 1. Data sync check
  # ----------------------------
  - name: "init-data-sync"
    description: "Synchronize data/ to .ai/ at session start"
    enabled: true
    matcher: "startup"  # Only on new sessions
    hooks:
      - type: "command"
        timeout: 30
        command: |
          echo "🔧 [data-sync] Vérification de synchronisation..."

          DATA_DIR="prompts/fr/metametaprompts/data"
          AI_DIR=".ai"

          # Check if data/ has changes not in .ai/
          if [ -d "$DATA_DIR/skills" ] && [ -d "$AI_DIR/skills" ]; then
            DATA_HASH=$(find "$DATA_DIR/skills" -name "*.yaml" -exec cat {} \; 2>/dev/null | md5sum | cut -d' ' -f1)
            AI_HASH=$(find "$AI_DIR/skills" -name "*.yaml" -exec cat {} \; 2>/dev/null | md5sum | cut -d' ' -f1)

            if [ "$DATA_HASH" != "$AI_HASH" ]; then
              echo "⚠️ [data-sync] Changements détectés dans data/, synchronisation nécessaire"
              echo "   Exécutez: cp -r $DATA_DIR/skills/* $AI_DIR/skills/"
            else
              echo "✅ [data-sync] Skills synchronisés"
            fi
          fi

          # Check hooks sync
          if [ -f "$DATA_DIR/hooks/hooks.yaml" ] && [ -f "$AI_DIR/hooks/hooks.yaml" ]; then
            DATA_HOOKS_HASH=$(md5sum "$DATA_DIR/hooks/hooks.yaml" | cut -d' ' -f1)
            AI_HOOKS_HASH=$(md5sum "$AI_DIR/hooks/hooks.yaml" | cut -d' ' -f1)

            if [ "$DATA_HOOKS_HASH" != "$AI_HOOKS_HASH" ]; then
              echo "⚠️ [data-sync] hooks.yaml désynchronisé, copie..."
              cp "$DATA_DIR/hooks/hooks.yaml" "$AI_DIR/hooks/hooks.yaml"
              echo "✅ [data-sync] hooks.yaml synchronisé"
            fi
          fi

  # ----------------------------
  # 2. Generate check
  # ----------------------------
  - name: "init-generate-check"
    description: "Check if generate.sh needs to run"
    enabled: true
    matcher: "startup|resume"
    hooks:
      - type: "command"
        timeout: 60
        command: |
          echo "🔧 [generate] Vérification des fichiers générés..."

          if [ -f ".ai/generate.sh" ]; then
            if ! .ai/generate.sh --check 2>/dev/null; then
              echo "⚠️ [generate] Régénération nécessaire..."
              .ai/generate.sh 2>/dev/null && echo "✅ [generate] Fichiers régénérés" || echo "❌ [generate] Échec"
            else
              echo "✅ [generate] Fichiers à jour"
            fi
          fi

  # ----------------------------
  # 3. Load memory context
  # ----------------------------
  - name: "init-load-memory"
    description: "Load project memory into context"
    enabled: true
    hooks:
      - type: "command"
        command: |
          echo "🧠 [memory-keeper] Chargement du contexte..."

          if [ -f ".ai/MEMORY.md" ]; then
            echo ""
            echo "═══════════════════════════════════════════════════════════════"
            echo "                    CONTEXTE PROJET CHARGÉ"
            echo "═══════════════════════════════════════════════════════════════"
            cat ".ai/MEMORY.md"
            echo "═══════════════════════════════════════════════════════════════"
            echo ""
          else
            echo "⚠️ [memory-keeper] Fichier .ai/MEMORY.md non trouvé"
          fi

  # ----------------------------
  # 4. Self-improver quick check
  # ----------------------------
  - name: "init-self-improver"
    description: "Quick check for rule/skill changes"
    enabled: true
    matcher: "startup"
    hooks:
      - type: "command"
        timeout: 10
        command: |
          echo "🔄 [self-improver] Vérification des changements de règles..."

          # Check AGENTS.md hash vs stored hash
          if [ -f "AGENTS.md" ] && [ -f ".ai/VERSION" ]; then
            CURRENT_HASH=$(md5sum AGENTS.md | cut -d' ' -f1)
            STORED_INFO=$(cat .ai/VERSION)

            # Simple check - if AGENTS.md changed since last generate
            if ! echo "$STORED_INFO" | grep -q "agents"; then
              echo "ℹ️ [self-improver] Première session ou version mise à jour"
            fi
          fi

          echo "✅ [self-improver] Vérification terminée"

# ============================================================================
# USER PROMPT SUBMIT - À chaque message utilisateur·ice
# ============================================================================

UserPromptSubmit:
  - name: "user-context-reminder"
    description: "Rappel léger du contexte"
    enabled: true
    hooks:
      - type: "command"
        command: |
          # Feedback visuel discret
          echo "📋 Session active | Mémoire: .ai/MEMORY.md"

# ============================================================================
# PRE TOOL USE - Avant l'exécution d'un outil
# ============================================================================

PreToolUse:
  # ----------------------------
  # Guardrails - Fichiers sensibles
  # ----------------------------
  - name: "protect-env-files"
    description: "Require confirmation before writing to .env files"
    enabled: true
    matcher: "Write"
    hooks:
      - type: "command"
        command: |
          INPUT=$(cat)
          FILE_PATH=$(echo "$INPUT" | grep -o '"file_path"[[:space:]]*:[[:space:]]*"[^"]*"' | sed 's/.*"file_path"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/')

          if echo "$FILE_PATH" | grep -qE '\.env'; then
            echo '{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"ask","permissionDecisionReason":"⚠️ Écriture dans un fichier .env détectée. Contient potentiellement des secrets."}}'
            exit 0
          fi
          exit 0

  - name: "protect-credentials"
    description: "Block writes to credential files"
    enabled: true
    matcher: "Write"
    hooks:
      - type: "command"
        command: |
          INPUT=$(cat)
          FILE_PATH=$(echo "$INPUT" | grep -o '"file_path"[[:space:]]*:[[:space:]]*"[^"]*"' | sed 's/.*"file_path"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/')

          if echo "$FILE_PATH" | grep -qiE 'credential|secret|password|token|key\.'; then
            echo '{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"ask","permissionDecisionReason":"⚠️ Écriture dans un fichier de credentials détectée."}}'
            exit 0
          fi
          exit 0

  # ----------------------------
  # Guardrails - Opérations destructives
  # ----------------------------
  - name: "protect-destructive-git"
    description: "Protect against destructive git operations"
    enabled: true
    matcher: "Bash"
    hooks:
      - type: "command"
        command: |
          INPUT=$(cat)
          COMMAND=$(echo "$INPUT" | grep -o '"command"[[:space:]]*:[[:space:]]*"[^"]*"' | sed 's/.*"command"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/')

          # Check for dangerous patterns
          if echo "$COMMAND" | grep -qE 'git (push.*--force|reset --hard|clean -[fd])'; then
            echo '{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"ask","permissionDecisionReason":"⚠️ Opération git destructive détectée. Confirmer?"}}'
            exit 0
          fi

          if echo "$COMMAND" | grep -qE 'rm -rf'; then
            echo '{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"ask","permissionDecisionReason":"⚠️ Suppression récursive détectée. Confirmer?"}}'
            exit 0
          fi
          exit 0

  # ----------------------------
  # Guardrails - Protection source de vérité
  # ----------------------------
  - name: "protect-generated-files"
    description: "Warn when editing generated files instead of source"
    enabled: true
    matcher: "Write|Edit"
    hooks:
      - type: "command"
        command: |
          INPUT=$(cat)
          FILE_PATH=$(echo "$INPUT" | grep -o '"file_path"[[:space:]]*:[[:space:]]*"[^"]*"' | sed 's/.*"file_path"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/')

          # Check if editing .ai/ directly (should edit data/ instead)
          if echo "$FILE_PATH" | grep -qE '^\.ai/(skills|hooks)/'; then
            echo '{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"ask","permissionDecisionReason":"⚠️ Modification directe de .ai/ détectée. Préférez modifier data/ (source de vérité)."}}'
            exit 0
          fi
          exit 0

# ============================================================================
# POST TOOL USE - Après l'exécution d'un outil
# ============================================================================

PostToolUse:
  # ----------------------------
  # Auto-regenerate on skill changes
  # ----------------------------
  - name: "auto-generate-on-skill-change"
    description: "Regenerate platform configs when skills are modified"
    enabled: true
    matcher: "Write"
    hooks:
      - type: "command"
        command: |
          INPUT=$(cat)
          FILE_PATH=$(echo "$INPUT" | grep -o '"file_path"[[:space:]]*:[[:space:]]*"[^"]*"' | sed 's/.*"file_path"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/')

          if echo "$FILE_PATH" | grep -qE '(data/skills|\.ai/skills)/.*\.yaml$'; then
            echo "📦 [generate] Skill modifié, régénération..."
            .ai/generate.sh 2>/dev/null && echo "✅ [generate] Régénéré" || echo "❌ [generate] Échec"
          fi

  # ----------------------------
  # Sync data to .ai on data changes
  # ----------------------------
  - name: "auto-sync-on-data-change"
    description: "Sync data/ to .ai/ when data files change"
    enabled: true
    matcher: "Write"
    hooks:
      - type: "command"
        command: |
          INPUT=$(cat)
          FILE_PATH=$(echo "$INPUT" | grep -o '"file_path"[[:space:]]*:[[:space:]]*"[^"]*"' | sed 's/.*"file_path"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/')

          # If data/hooks changed, sync to .ai/hooks
          if echo "$FILE_PATH" | grep -qE 'data/hooks/.*\.yaml$'; then
            echo "🔧 [data-sync] Hooks modifiés, synchronisation..."
            cp "prompts/fr/metametaprompts/data/hooks/hooks.yaml" ".ai/hooks/hooks.yaml" 2>/dev/null
            echo "✅ [data-sync] Hooks synchronisés vers .ai/"
          fi

          # If data/skills changed, sync to .ai/skills
          if echo "$FILE_PATH" | grep -qE 'data/skills/.*\.yaml$'; then
            SKILL_NAME=$(basename "$FILE_PATH")
            echo "🔧 [data-sync] Skill modifié: $SKILL_NAME"
            cp "$FILE_PATH" ".ai/skills/$SKILL_NAME" 2>/dev/null
            echo "✅ [data-sync] Skill synchronisé vers .ai/"
          fi

  # ----------------------------
  # Inclusive writing reminder
  # ----------------------------
  - name: "inclusivity-reminder-fr"
    description: "Remind about inclusive writing for French files"
    enabled: true
    matcher: "Write"
    hooks:
      - type: "command"
        command: |
          INPUT=$(cat)
          FILE_PATH=$(echo "$INPUT" | grep -o '"file_path"[[:space:]]*:[[:space:]]*"[^"]*"' | sed 's/.*"file_path"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/')

          if echo "$FILE_PATH" | grep -qE '/fr/.*\.md$'; then
            echo "🔍 [inclusivity] Contenu FR modifié. Rappel: écriture inclusive requise."
          fi

  # ----------------------------
  # Translation sync reminder
  # ----------------------------
  - name: "translation-sync-reminder"
    description: "Remind to sync translations when docs change"
    enabled: true
    matcher: "Write"
    hooks:
      - type: "command"
        command: |
          INPUT=$(cat)
          FILE_PATH=$(echo "$INPUT" | grep -o '"file_path"[[:space:]]*:[[:space:]]*"[^"]*"' | sed 's/.*"file_path"[[:space:]]*:[[:space:]]*"[^"]*\)".*/\1/')

          if echo "$FILE_PATH" | grep -qE 'docs/en/.*\.md$'; then
            FR_PATH=$(echo "$FILE_PATH" | sed 's|docs/en/|docs/fr/|')
            echo "🌍 [translator] Doc EN modifié. Vérifier traduction FR: $FR_PATH"
          fi

# ============================================================================
# STOP - Quand Claude finit de répondre
# ============================================================================
# Ce hook vérifie si des actions de consistance sont nécessaires.
# Si oui, il retourne { "ok": false, "reason": "..." } pour continuer.

Stop:
  - name: "consistency-check"
    description: "Check if memory or consistency actions are needed"
    enabled: true
    hooks:
      - type: "agent"
        timeout: 60
        prompt: |
          Tu es l'agent de vérification de consistance du projet Meta-prompt-LLM.

          ## Contexte

          Tu es appelé à la fin de chaque réponse de Claude pour vérifier
          si des actions de consistance sont nécessaires.

          ## Ta mission

          1. Lis le transcript de la session via $ARGUMENTS
          2. Détermine si cette session a impliqué:
             - Des décisions techniques importantes
             - Des modifications de règles ou skills
             - Des leçons apprises
             - Des changements de contexte projet

          3. Si OUI à l'un de ces points:
             - Retourne { "ok": false, "reason": "🧠 [memory-keeper] Mise à jour de .ai/MEMORY.md recommandée: [résumé des changements]" }

          4. Si NON (conversation simple, pas de décision):
             - Retourne { "ok": true }

          ## Critères de décision

          METTRE À JOUR MEMORY.MD si:
          - Nouvelle décision technique prise
          - Nouveau skill créé ou modifié
          - Nouvelle règle ou convention adoptée
          - Problème résolu qui constitue une leçon
          - Changement de contexte projet

          NE PAS METTRE À JOUR si:
          - Simple question/réponse
          - Lecture de fichiers sans modification
          - Exécution de commandes routinières
          - Correction de typos mineurs

          ## Format de réponse

          Retourne UNIQUEMENT un objet JSON:
          { "ok": true }
          ou
          { "ok": false, "reason": "🧠 [memory-keeper] [description de ce qui doit être ajouté à MEMORY.md]" }

  # ----------------------------
  # Session status summary (after consistency-check)
  # ----------------------------
  - name: "session-status-summary"
    description: "Display visual summary of skills, hooks, and commands"
    enabled: true
    hooks:
      - type: "command"
        timeout: 10
        command: |
          # Session Status Summary - Generated by session-status skill
          # This provides visibility into the system's capabilities

          echo ""
          echo "---"
          echo ""
          echo "## 📊 SYNTHÈSE SYSTÈME META-PROMPT-LLM"
          echo ""

          # Count and list skills
          SKILLS_DIR=".ai/skills"
          if [ -d "$SKILLS_DIR" ]; then
            SKILL_COUNT=$(find "$SKILLS_DIR" -name "*.yaml" ! -name "_TEMPLATE.yaml" | wc -l)
            echo "### 🎯 Skills ($SKILL_COUNT)"
            echo ""
            echo "| Skill | Description |"
            echo "|-------|-------------|"

            for skill_file in "$SKILLS_DIR"/*.yaml; do
              [ -f "$skill_file" ] || continue
              [ "$(basename "$skill_file")" = "_TEMPLATE.yaml" ] && continue

              skill_name=$(grep -m1 '^name:' "$skill_file" | sed 's/name:[[:space:]]*"\?\([^"]*\)"\?/\1/')
              skill_desc=$(grep -m1 '^description:' "$skill_file" | sed 's/description:[[:space:]]*"\?\([^"]*\)"\?/\1/' | cut -c1-40)

              [ -n "$skill_name" ] && echo "| \`$skill_name\` | $skill_desc |"
            done
          fi

          echo ""
          echo "### 🔄 Hooks Lifecycle"
          echo ""
          echo "\`\`\`"
          echo "SessionStart ─┬─ 📋 platform-detect"
          echo "              ├─ 🔧 data-sync"
          echo "              ├─ 🔧 generate-check"
          echo "              ├─ 🧠 load-memory"
          echo "              └─ 🔄 self-improver"
          echo "      ↓"
          echo "UserPromptSubmit ── 📋 context-reminder"
          echo "      ↓"
          echo "PreToolUse ───── 🛡️ guardrails (.env/secrets/git/rm)"
          echo "      ↓"
          echo "PostToolUse ──┬─ 📦 auto-generate"
          echo "              ├─ 🔧 auto-sync"
          echo "              ├─ 🔍 inclusivity"
          echo "              └─ 🌍 translator"
          echo "      ↓"
          echo "Stop ─────────┬─ ✅ consistency-check [AGENT]"
          echo "              └─ 📊 session-status [CMD]"
          echo "      ↓"
          echo "SessionEnd ───── 📝 final-reminder"
          echo "\`\`\`"
          echo ""
          echo "### 📋 Commandes"
          echo ""
          echo "\`\`\`bash"
          echo ".ai/generate.sh          # Régénère tout"
          echo ".ai/generate.sh --check  # Vérifie"
          echo "cat .ai/MEMORY.md        # Mémoire"
          echo "\`\`\`"
          echo ""

# ============================================================================
# SESSION END - Quand la session se termine
# ============================================================================

SessionEnd:
  - name: "final-memory-reminder"
    description: "Final reminder to update memory"
    enabled: true
    hooks:
      - type: "command"
        command: |
          echo ""
          echo "═══════════════════════════════════════════════════════════════"
          echo "                    FIN DE SESSION"
          echo "═══════════════════════════════════════════════════════════════"
          echo "📝 Si des décisions importantes ont été prises,"
          echo "   assurez-vous que .ai/MEMORY.md est à jour."
          echo ""
          echo "🔄 Pour la prochaine session:"
          echo "   - SessionStart chargera automatiquement le contexte"
          echo "   - data-sync vérifiera la synchronisation"
          echo "═══════════════════════════════════════════════════════════════"

# ============================================================================
# VISUAL FEEDBACK REFERENCE
# ============================================================================
#
# Cette section documente les conventions de feedback visuel utilisées.
#
# STRUCTURE D'UN MESSAGE DE FEEDBACK:
#   [emoji] [agent] [action] [détails]
#
# EMOJIS PAR CATÉGORIE:
#   ✅ Succès
#   ⚠️ Attention requise
#   ❌ Échec
#   📋 Information
#   🔧 Maintenance/sync
#   🧠 Mémoire
#   🔄 Auto-amélioration
#   📦 Génération
#   🔍 Vérification
#   🌍 Traduction
#   📊 Orchestration
#
# AGENTS IDENTIFIÉS:
#   [data-sync]          - Synchronisation data/ → .ai/
#   [generate]           - Génération de fichiers
#   [memory-keeper]      - Gestion mémoire
#   [self-improver]      - Auto-amélioration
#   [consistency-check]  - Vérification post-réponse
#   [inclusivity]        - Écriture inclusive
#   [translator]         - Synchronisation EN/FR
#
# IMBRICATION (THREAD LEVEL):
#   Niveau 0: Agent principal (Claude)
#   Niveau 1: └── Sous-agent (invoqué par Claude)
#   Niveau 2:     └── Sous-sous-agent (invoqué par sous-agent)
#
# Exemple de sortie:
#   🔧 [data-sync] Vérification de synchronisation...
#   ├── Checking skills: data/skills/*.yaml
#   ├── Checking hooks: data/hooks/hooks.yaml
#   └── ✅ Synchronisation complète
#
# ============================================================================
